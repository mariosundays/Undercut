__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/
.idea/
.vscode/*
# ...but share the watcher exclusions; without them VS Code indexes the
# 400 MB PyInstaller output and hangs. (Must ignore .vscode/* not .vscode/ -
# git will not descend into an ignored DIRECTORY to un-ignore a file.)
!.vscode/settings.json
releases/

# Superseded two-track prototype, kept locally for reference only.
_v2_twotrack/

# Test artefacts
*_test.mp4
*_thumb.mp4

# PyInstaller writes this from build.bat
*.spec

# Local PyInstaller output (CI builds the real thing)
ffmpeg_bin/
